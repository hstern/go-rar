# Test binaries and coverage output.
*.test
*.out
coverage.*

# Compiled output of the example/three-flow demo program (its binary
# shares the directory name, so `go build .` drops it next to main.go).
/example/three-flow/three-flow

# Editor / IDE state.
.idea/
.vscode/
*.swp
*.swo

# macOS / Windows filesystem cruft.
.DS_Store
Thumbs.db
