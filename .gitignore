# Test binaries and coverage output.
*.test
*.out
coverage.*

# Compiled output of the example demo programs (each binary shares
# the directory name, so `go build .` drops it next to main.go).
/example/three-flow/three-flow
/example/fosite-handler/fosite-handler

# Editor / IDE state.
.idea/
.vscode/
*.swp
*.swo

# macOS / Windows filesystem cruft.
.DS_Store
Thumbs.db
